# Como compilar

```.
Para el programa tcp-cli.cpp en la carpeta src abra una terminal y ejecute:
 g++ SSLSocket.cpp VSocket.cpp Socket.cpp tcp-cli.cpp -o tcp-cli.out -lssl -lcrypto
./tcp-cli.out
puede ingresar seguido valores para indicar si es ipv4 o 6 y un 0 o 1 para indicar si usa ssl


```
